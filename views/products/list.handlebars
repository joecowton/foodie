<h1>Products</h1>
{{#each products}}
  <div class="card card-body mb-3 ">
    <h4>{{title}}</h4>
    <p>{{expiryDate}}</p>
    <p>{{image}}</p>
  </div>
{{else}}
<p> No products listed </p>
{{/each}}
